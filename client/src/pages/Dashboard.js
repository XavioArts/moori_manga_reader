import React from "react";

const Dashboard = () => {
    return (
        <div>
            <h1>Dashboard page</h1>
            <p>
                This will have:
                -The users info, wallet, and achievements
                -The users cards owned, and badges owned 
                -Currently reading
                -Feed (i.e. new chapters from your library, comments & replies, etc.)
            </p>
        </div>
    )
}

export default Dashboard;