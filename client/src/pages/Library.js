import React from "react";

const Library = () => {
    return (
        <div>
            <h1>Library page</h1>
            <p>
                This will have:
                -The comics the user has added to library and are reading
                -The ability to read comics
                -Should show which chapter you are on for each one (?)
            </p>
        </div>
    )
}

export default Library;